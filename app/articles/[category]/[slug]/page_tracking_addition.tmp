import ArticleTracking from '@/components/analytics/ArticleTracking'

// Add inside the <main> tag, before other content:
<ArticleTracking 
  articleId={article.id} 
  title={article.title} 
  category={article.categories?.name || ''} 
  author={article.authors?.name || ''} 
/>
